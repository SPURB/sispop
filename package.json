{
  "name": "sispop",
  "version": "1.0.0",
  "description": "Geração de documentos para SISPOP da São Paulo Urbanismo",
  "author": "dev-spurbanismo",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=dev nuxt",
    "build": "cross-env NODE_ENV=prod nuxt generate",
    "build:homolog": "cross-env NODE_ENV=homolog nuxt generate",
    "serve": "nuxt serve",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "lint:fix": "eslint --fix --ext .js,.vue --ignore-path .gitignore ."
  },
  "dependencies": {
    "@nuxtjs/pwa": "^3.0.0-beta.20",
    "@spurb/componentes": "0.0.1",
    "axios": "^0.21.0",
    "nuxt": "^2.14.0"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config": "^3.1.0",
    "@nuxtjs/eslint-module": "^2.0.0",
    "cross-env": "^7.0.2",
    "babel-eslint": "^10.1.0",
    "eslint": "^7.5.0",
    "eslint-plugin-nuxt": "^1.0.0",
    "node-sass": "^4.12.0",
    "sass-loader": "^8.0.0"
  }
}
